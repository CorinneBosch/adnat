<nav class='bar-box'>
  <div class="some-collapse-navbar">
    <li class='logo-li'>
      <% if user_signed_in? %>
        <%# <% user_org_id = current_user.organisation_id %>
        <%= link_to image_tag('lonely-boy.gif', :class => 'logo'),
            (organisations_path + "/#{current_user.organisation_id}") %>
      <% else %>
        <%= link_to image_tag('lonely-boy.gif', :class => 'logo'), root_path %>
      <% end %>
    </li>
    <% if user_signed_in? %>
      <li class="li-box">
        <%= link_to "Log out", destroy_user_session_path, method: :delete %>
      </li>
        <% if current_user.organisation_id != nil %>
          <li class='li-box'>
            <%= link_to "Leave", users_leave_organisation_path,
                         method: :delete,
                         remote: true,
                         data: { confirm: "Are you sure?" } %>
          </li>
        <% end %>
      <%# <li class="li-box"> %>
        <p id='vp'>Logged in as <%= current_user.name %></p>
      <%# </li> %>
    <% else %>
      <li class="li-box">
        <%= link_to "Log In", new_user_session_path %>
      </li>
      <li class="li-box">
        <%= link_to "Sign Up", new_user_registration_path %>
      </li>
    <% end %>
  </div>
</nav>