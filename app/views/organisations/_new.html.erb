<div class='edit-box'>
  <%= form_with(model: @organisation, remote: true, ) do |form| %>

    <% if @organisation.errors.any? %>
    <div id='notice' >
      <% @organisation.errors.full_messages.each do |msg| %>
        <%= msg %>
      <% end %>
    </div>
    <% end %>

    <div>
      <%= form.label :name %><br>
      <%= form.text_field :name, id: "name", :class => 'center', required: true  %>
    </div>

    <div>
      <%= form.label 'Hourly rate £' %><br>
      <%= form.text_field :hourly_rate, id: "hourly_rate", :class => 'center', required: true  %>
    </div>
    <div>
      <%= form.submit 'Join', :id => 'create-join', :class => 'sml-button' %>
    </div>
  <% end %>
</div>

