<p>Logged in as <%= current_user.name %></p>
<h2>Organisations</h2>

<% if !is_org_member? %>
  <p>You aren't a member of any organisation yet</p>
  <p>Join an existing organisation or create a new one</p>
<% end %>

<ul>
  <% @organisations.each do |org| %>
    <li>
      <%= org.name %>
 
      <% org_id = org.id %>
      <%= button_to "join", users_join_organisation_path(@user, org_id: org_id),
                                   method: :post,
                                   remote: true %>

      <div id=<%= "edit-org-#{org.id}" %> >
        <%= link_to "Edit", edit_organisation_path(@organisation, id: org_id), remote: true  %>
      </div>
    </li>
  <% end %>
</ul>

<h2>Create new Organisation</h2>

<%= current_user.id %>

<%= render partial: "form" %> 
