<h2>Organisations</h2>

<ul>
  <% @organisations.each do |org| %>
    <li>
      <%= org.name %>
      <%= link_to "join", organisation_path(org) %>
      <%= link_to "edit", edit_organisation_path(org) %>
    </li>
  <% end %>
</ul>

<h2>Create new Organisation</h2>

<%= render partial: "form" %> 
<%# <%= render "form", organisation: @organisation %> 
