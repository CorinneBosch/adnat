<h2>Edit <%= @organisation.name %></h2><br>

<%= form_with(model: @organisation, remote: true, ) do |form| %>

  <% if @organisation.errors.any? %>
    <% @organisation.errors.full_messages.each do |msg| %>
      <%= msg %>
    <% end %>
  <% end %>

  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name, :class => 'center', required: true  %>
  </div>

  <div>
    <%= form.label 'Hourly rate £' %><br>
    <%= form.text_field :hourly_rate, :class => 'center', required: true  %>
  </div>
  <br>
  <div>
    <%= form.submit 'Update', :class => 'sml-edit' %>
  </div>
<% end %>

<%= button_to "Delete", organisation_path(@organisation),
            :class => 'sml-join',
            method: :delete,
            data: { confirm: "Are you sure?" } %> 

