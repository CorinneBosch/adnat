<h2>Create new Organisation</h2>

<%= form_with model: @organisation do |form| %>
<%# <%= form_for @organisation, url: new_organisation_path do |form| %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name, required: true %>
  </div>

  <div>
    <%= form.label :hourly_rate %><br>
    <%= form.text_field :hourly_rate, required: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>